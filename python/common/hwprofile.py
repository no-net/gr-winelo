hwprofile = {
    # amplitude of the Gaussian noise that is added to the stream
    'noise_ampl':0,
    # factors of the IQ-imbalance
    'inphase_ampl':1,
    'quadrature_ampl':1,
    # phase noise ampl and history
    'phase_noise_ampl':0,
    'phase_noise_hist':0,
    # frequency offset per sample in radians
    'freq_offset':0,
    # the current phase vector keeps track of the phase throughout all samples
    'cur_phase':1,
    }
